# Importacion del modelo usuario en django
# este es para la creacion de usuarios por parte de DJANDO
from django.contrib.auth.models import User

from django.db import models

# esta es para la creacion de usuarion personalizados, sin embargo con las caracteristicas de DJANGO
from django.contrib.auth.models import AbstractUser

# creacion del modelo de registro del Login
